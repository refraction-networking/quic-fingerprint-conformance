# Conformance harness (Python 3.9+). The reference implementation is pyquicfp,
# pulled from the org repo; it brings in scapy + cryptography.
pyquicfp @ git+https://github.com/refraction-networking/pyquicfp.git
